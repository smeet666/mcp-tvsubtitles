/**
 * Kept in step with the version in package.json. The User-Agent embeds it, so
 * tvsubtitles.net can tell which build is calling from its own logs.
 */
export const PKG_VERSION = "0.3.0";
export const REPO_URL = "https://github.com/smeet666/mcp-tvsubtitles";
